Options -Indexes

# Route all non-file requests to index.html
RewriteEngine On
RewriteBase /sbg/admin/
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . index.html [L]
